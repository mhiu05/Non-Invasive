apiVersion: v1
kind: Service
metadata:
  name: {{ include "rppg-backend.fullname" . }}
  labels:
    app: {{ include "rppg-backend.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "rppg-backend.name" . }}
