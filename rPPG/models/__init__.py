# Import từng model theo yêu cầu — không import tất cả ở đây
# để tránh lỗi khi thiếu dependency (timm, mamba-ssm, ...)
#
# Ví dụ sử dụng:
#   from models.DeepPhys import DeepPhys
#   from models.TSCAN import TSCAN
